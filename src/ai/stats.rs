pub struct SearchStatistics {
    pub num_iters: i32,
    pub num_position_evals: i32,
    pub num_cutoffs: u64,
    pub eval: i32,
    pub num_checks: i32,
    pub num_mates: i32,
    pub is_book: bool,
}